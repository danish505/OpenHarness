# Project-scoped Codex configuration.
# Keep this lightweight for fast startup.

[agents]
max_threads = 4
max_depth = 1
job_max_runtime_seconds = 1800

# No MCP servers are required for the v1 harness.
